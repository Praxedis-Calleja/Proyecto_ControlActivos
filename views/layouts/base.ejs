  <!doctype html>
  <html lang="es">
  <head>
    <meta charset="utf-8">
    <title>Control de Activos</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/css/main.css" rel="stylesheet">
  </head>
  <body>
    <header>
      <h1>Control de Activos</h1>
      <div class="header-actions">
        <% if (user) { %>
          <a class="btn" href="/activos">📋 Lista de activos</a>
          <a class="btn" href="/incidencias">📄 Incidencias</a>
          <a class="btn" href="/bajas">📉 Reportes de baja</a>
          <a class="btn" href="/incidencias/nueva">🛠️ Registrar incidencia</a>
        <% } %>

        <% if (user) { %>
          <form action="/logout" method="post" class="logout-form">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button type="submit">🚪 Cerrar sesión (<%= [user?.nombre, user?.apellido].filter(Boolean).join(' ') || user?.nombre || 'Cuenta' %>)</button>
          </form>
        <% } %>
      </div>
    </header>
    <main>
      <%- typeof content === 'string' ? content : '' %>
      <%- typeof body !== 'undefined' ? body : '' %>
    </main>
  </body>
  </html>
